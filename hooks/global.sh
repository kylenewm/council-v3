#!/bin/bash
# Minimal global rules - injected for ALL modes
# Keep this SHORT - mode scripts add specifics

cat << 'EOF'
[GLOBAL]
Push back if approach seems wrong, scope too large, or requirements unclear.
Investigate before assuming. Ask if uncertain.
EOF

exit 0
